addpath("recon\");

pth = "datasets/64/fid";
fid = fopen(pth);
data = fread(fid,"int32");
fclose(fid);

data = complex(data(1:2:end),data(2:2:end));
data = reshape(data,[128 128 31]);
imgs = fftcn(data,[1 2]);